# Applications

- **`desktop_pipeline_demo/`**: Desktop perception pipeline demo (webcam / file).
- **`cesium_viewer/`**: Phase 4 CesiumJS 3D digital twin (Vite). See its README.

CLI / showcase tools live in [`tools/`](../tools/) (`curv_cli`,
`video_pipeline_demo`, `live_snapshot_demo`, `synth_generator`, `evaluate`,
`benchmark`).
